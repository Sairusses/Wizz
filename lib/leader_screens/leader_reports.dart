import 'package:flutter/material.dart';

class ReportsLeader extends StatelessWidget{
  const ReportsLeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text('Leader Reports'),
        )
    );
  }

}